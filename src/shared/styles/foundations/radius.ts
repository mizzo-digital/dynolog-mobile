const RADIUS_BASE = 4;

export const radius = {
  none: 0,
  xs: `${1 * RADIUS_BASE}px`,
  sm: `${1.5 * RADIUS_BASE}px`,
  base: `${2 * RADIUS_BASE}px`,
  md: `${3 * RADIUS_BASE}px`,
  lg: `${4 * RADIUS_BASE}px`,
  full: `${10000 * RADIUS_BASE}px`,
};
